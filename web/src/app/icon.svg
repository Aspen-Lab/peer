<svg width="64" height="64" viewBox="0 0 64 64" fill="none" xmlns="http://www.w3.org/2000/svg">
  <!-- Peer's mark, exactly as the masthead draws it (components/shell/
       masthead.tsx, Mark): the sheet with its registered corner in the
       accent. The masthead's sheet takes currentColor; the tab has no
       colour to inherit, so it is the fixed black the chrome's ink sits on. -->
  <rect x="4" y="4" width="56" height="56" fill="#1d1d1d" />
  <g fill="#ff520d">
    <path d="M30 4h30v10H30z" />
    <path d="M50 4h10v30H50z" />
  </g>
</svg>
